interface CivObject {
    name: string //i.e. "Aztecs"
    logo: string //name of image file. components handle the path. 
}

// check later to see if this is up to date with civJSON
// kind of hard to strictly define all this up front.