import React from "react";

export class Timeline extends React.Component {


    // render a bunch of divs that are clickable
    // with a time on the left, storing an array
    // of what exists on that time (and accessing what's before?) or just store multiple times
    Timeline() {

    }

    render() {
        return (
            0
        )
    }
}